// ~~ Generated by projen. To modify, edit .projenrc.ts and run "pnpm dlx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for PostDeployFunction
 */
export interface PostDeployFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/lambdas/post-deploy/post-deploy.
 */
export class PostDeployFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: PostDeployFunctionProps) {
    super(scope, id, {
      description: 'src/lambdas/post-deploy/post-deploy.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs22.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/lambdas/post-deploy/post-deploy.lambda')),
    });
  }
}