packages:
  - "**"
  # prevent pnpm from trying to install dependencies in cdk asset directories
  - "!**/cdk.out/**"
  # prevent turbo from thinking workspace already exists and erroring
  - "!**/.next/**"
  - "!pruned/**"
