{
  "name": "cdk-nextjs-example-turbo",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "dotenv -- cdk deploy --require-approval never --outputs-file outputs.json",
    "destroy": "dotenv -- cdk destroy --force",
    "deploy:ci": "cdk deploy --require-approval never --outputs-file outputs.json",
    "destroy:ci": "cdk destroy --force"
  },
  "dependencies": {
    "aws-cdk": "catalog:",
    "aws-cdk-lib": "catalog:",
    "cdk-nextjs": "link:../..",
    "constructs": "catalog:",
    "tsx": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:"
  }
}
