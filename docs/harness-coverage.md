# Next.js compatibility harness — coverage record

What has been run from [vercel/next.js's own e2e suite][harness] against a real
cdk-nextjs deployment, what happened, and — for anything that failed — why, plus
an explicit verdict on whether the failure is acceptable or a bug to fix.

`test/deploy-tests-manifest.json` is the machine-readable half: its
`rules.include` (whole files) and `suites` (a file minus named cases) are what
the harness actually runs, and nothing goes in either that has not been watched
to pass. Its `excluded-notes` carries a one-line verdict for everything
deliberately left out, pointing back here. This file is the human-readable half.
See
`scripts/e2e-harness/README.md` for how the harness works and how to screen a
new candidate.

## Verdict key

| Verdict          | Meaning                                                                           |
| ---------------- | --------------------------------------------------------------------------------- |
| **pass**         | Runs and passes against a real deployment. Listed in `rules.include`.             |
| **fixed**        | Failed, was root-caused to a cdk-nextjs defect, and the defect has been fixed.   |
| **bug**          | Fails for a cdk-nextjs reason. Not acceptable. Fix it, then add the file.         |
| **unsupported**  | Fails because of a deliberate, documented product limitation. Acceptable.        |
| **CDN-inherent** | Fails for a reason true of any CDN-fronted deployment, not just this one. Acceptable. |
| **architectural** | Fails for a reason true of any distributed (multi-instance) cache handler. Acceptable. |
| **no signal**    | next.js skips or gates the file itself. Nothing was measured either way.          |

"Acceptable" means the failure is understood and is not worth fixing — it is not
a reason to stop widening the list elsewhere. Every **bug** row is a defect that
should come back as a regression test once fixed.

## Status

As of 2026-09-23, against next.js 16.3.5. The screening funnel is recorded in
the manifest's `screening` block and regenerated by
`node scripts/e2e-harness/screen.mjs --next ../next.js --write`:

| Of next.js's 1134 e2e files   | Count |
| ----------------------------- | ----- |
| edge-free (the hard ceiling)  | 931   |
| candidates after every screen | 246   |
| deployed and screened so far  | 258   |
| in `rules.include`            | 214   |
| plus, per-case, in `suites`   | 3     |

`candidates` falls as files leave it for `rules.include`, and it fell by 36 more
when the `skipDeployment` screen learned to read through a shared helper (see
"No signal" below). Roughly 190 candidates have never been deployed. The per-file
cost did fall once the cache bucket's `Tags` stopped forcing a full CloudFormation
deploy (see `scripts/e2e-harness/README.md`): batch 10's first file took 238s
making that one transitional change, and every run since has settled at 95–140s,
so what is left is ~6 hours of wall clock.

Of the 258 screened (25 of which turned out to deploy nothing — see
`skipDeployment` below):

| Verdict          | Files  |
| ---------------- | ------ |
| pass             | 214 whole files, plus 6 of 8 `trailingslash`, 3 of 5 `resume-data-cache` and 3 of 7 `dynamic-route-interpolation` cases |
| fixed            | 23 harness defects, every one of which came from a file listed above; 22 verified green against a deployment, defect 24 (a percent-encoded `public/` filename through `/_next/image`) queued for the next batch. Defect 23 is numbered in the same sequence but is *not* a harness defect — it came from `examples/e2e-tests`; see `docs/plans/adapter-runtime-progress.md` |
| bug              | 1 (`incremental-cache-path-traversal`) |
| awaiting verdict | 0 |
| unsupported      | 1 (`prerender-encoding`; separately, 203 files are disqualified by the edge screen and never deployed) |
| CDN-inherent     | 2 whole files, plus the 2 remaining `trailingslash` and 4 remaining `dynamic-route-interpolation` cases |
| architectural    | the 2 remaining `resume-data-cache` cases |
| no signal        | 28 (2 gated by next.js, 26 `skipDeployment` or stubbed in deploy mode) |

The twenty-three fixed harness defects are its whole return on investment so far.
All of them were real, all of them shipped, and none of them could have been
caught by the construct tests or by `examples/e2e-tests`.

## Passing — in `rules.include`

All cases in each file pass, on the first attempt, against a shared
`NextjsGlobalFunctions` deployment.

| File                                                            | What it covers                                    |
| --------------------------------------------------------------- | ------------------------------------------------- |
| `app-dir/actions-streaming`                                     | A server action returning a `ReadableStream`      |
| `app-dir/app-basepath`                                          | A `basePath` deployment, incl. action `redirect()` — 13 cases |
| `app-dir/app-client-cache/client-cache.defaults`                | Client router cache defaults                      |
| `app-dir/app-root-params-getters/generate-static-params`         | Root params read through `generateStaticParams`    |
| `app-dir/catchall-specificity`                                  | A catch-all competing with a more specific route  |
| `app-dir/dynamic-interception-route-revalidate`                  | An action calling `revalidatePath`, on an interception route |
| `app-dir/headers-static-bailout`                                | `headers()` forcing a static route dynamic        |
| `app-dir/interception-dynamic-segment`                          | An interception route over a dynamic segment      |
| `app-dir/layout-params`                                         | Params read in a layout, incl. an unfilled optional catchall — see defect 11 |
| `app-dir/metadata-icons`                                        | Icon metadata, incl. `/favicon.ico` and `apple-icon` |
| `app-dir/metadata-streaming`                                    | Streamed `<head>` metadata                        |
| `app-dir/prefetching-not-found`                                 | Prefetch of a `not-found` route                   |
| `app-dir/redirect-rewrite-dynamic`                              | `next.config` redirects/rewrites on dynamic routes |
| `app-dir/searchparams-static-bailout`                           | `searchParams` forcing a static route dynamic     |
| `app-dir/segment-cache/basic`                                   | Segment-level prefetch cache, core behavior       |
| `app-dir/segment-cache/cached-navigations`                       | Prefetch off the inlined app shell — 14 cases     |
| `app-dir/segment-cache/client-params`                           | Client-read params in cached segments             |
| `app-dir/segment-cache/deployment-skew`                         | `x-deployment-id` on HTML and RSC responses       |
| `app-dir/segment-cache/encoded-slash-params`                    | `%2F` inside a dynamic param                      |
| `app-dir/segment-cache/headers-keyed-caches`                    | Cache keyed on request headers                    |
| `app-dir/segment-cache/metadata`                                | Metadata in prefetched segments                   |
| `app-dir/segment-cache/no-prefetch`                             | `prefetch={false}`                                |
| `app-dir/segment-cache/prefetch-app-shell`                      | The app shell itself being prefetched              |
| `app-dir/segment-cache/prefetch-inlining`                       | Inlined prefetch hints, incl. across an on-demand revalidation — 15 cases, see defects 8 and 9 |
| `app-dir/segment-cache/prefetch-auto`                           | Default (`auto`) prefetching                      |
| `app-dir/segment-cache/prefetch-static-shell`                   | Static-shell prefetch                             |
| `app-dir/segment-cache/staleness/segment-cache-stale-time`      | `staleTime` expiry                                |
| `app-dir/segment-cache/vary-params`                             | Cache variance across params                      |
| `app-dir/server-actions-relative-redirect`                      | An action `redirect()` to a relative URL          |
| `app-dir/static-rsc-cache-components`                           | `cacheComponents` prerender served without a dynamic render |
| `app-dir/use-cache-private`                                     | `'use cache: private'`                            |
| `app-dir/use-params`                                            | `useParams()`, incl. `[id]/[id2]` — see defect 5  |
| `app-dir/catch-error`                                           | An error thrown in a server component, caught by `error.tsx` |
| `app-dir/error-boundary-navigation`                             | Navigating out of an error boundary — 7 cases, see defect 10 |
| `app-dir/global-error/basic`                                    | `global-error.tsx` replacing the whole document   |
| `app-dir/interception-dynamic-single-segment`                    | An interception route over one dynamic segment    |
| `app-dir/metadata-navigation`                                   | Metadata across client navigations — 7 cases, see defect 10 |
| `app-dir/not-found-with-pages-i18n`                             | App Router `not-found` in an app with i18n Pages  |
| `app-dir/optimistic-routing`                                    | Optimistic client navigation                      |
| `app-dir/pages-router-app-not-found`                            | A Pages Router 404 beside an App Router one       |
| `app-dir/parallel-routes-root-param-dynamic-child`               | A root-params app with no `app/layout.tsx` — 14 cases, see defect 13 |
| `app-dir/router-autoscroll`                                     | Scroll restoration on navigation — flaky, see below |
| `app-dir/shallow-routing`                                       | `history.pushState`/`replaceState` without a nav  |
| `app-dir/static-siblings`                                       | Sibling static routes under one layout            |
| `app-dir/sub-shell-generation`                                  | Per-locale PPR fallback shells, resumed — see defect 14 |
| `app-dir/use-selected-layout-segment-s`                          | `useSelectedLayoutSegment(s)`                     |
| `app-dir/actions-revalidate-remount`                            | An action revalidating a route it is mounted in   |
| `app-dir/app-catch-all-optional`                                | An optional catch-all at the app root             |
| `app-dir/app-routes-client-component`                           | A route handler importing a client component      |
| `app-dir/conflicting-search-and-route-params`                    | A `searchParams` key colliding with a route param |
| `app-dir/dynamic-requests`                                      | `cookies()`/`headers()` forcing a dynamic render  |
| `app-dir/external-redirect`                                     | `redirect()` to an off-origin URL                 |
| `app-dir/forbidden/basic`                                       | `forbidden()` and `forbidden.tsx`                 |
| `app-dir/global-not-found/basic`                                | `global-not-found.tsx`                            |
| `app-dir/hello-world`                                           | The smallest App Router app — the smoke test      |
| `app-dir/interception-routes-root-catchall`                      | An interception route under a root catch-all      |
| `app-dir/metadata-image-files`                                  | `opengraph-image`/`twitter-image` files           |
| `app-dir/metadata-static-file/metadata-static-file-root-route`    | A static metadata file at the root — see defect 12 |
| `app-dir/metadata-svg-icon`                                     | An SVG `icon` file                                |
| `app-dir/partial-fallback-root-blocking`                        | A blocking root shell under PPR — see defect 14   |
| `app-dir/partial-fallback-shell-upgrade`                        | A PPR shell replaced by a fuller one — see defect 14 |
| `404-page-app`                                                  | A Pages Router `pages/404` beside an App Router app — see defect 15 |
| `app-dir/default-error-page-ui`                                 | The built-in error page's markup                  |
| `app-dir/javascript-urls`                                       | `javascript:` hrefs, which must not navigate      |
| `app-dir/parallel-route-not-found`                              | `not-found` inside a parallel route slot          |
| `app-dir/static-generation-status`                              | The status code a statically generated route returns |
| `app-dir/use-cache-metadata-route-handler`                      | `'use cache'` in a metadata route handler         |
| `app-dir/use-router-bfcache-id`                                 | The router's bfcache identity across navigations  |
| `auto-export`                                                   | Pages Router automatic static optimization        |
| `invalid-href`                                                  | Malformed `href` values passed to `<Link>`        |
| `link-ref-app`                                                  | `ref` forwarded through `<Link>` in the App Router |
| `router-is-ready`                                               | Pages Router `router.isReady`                     |
| `app-dir/asset-prefix`                                          | A path-style `assetPrefix` — 7 cases, see defect 16 |
| `app-dir/asset-prefix-with-basepath`                            | `assetPrefix` applied on top of `basePath` — see defect 16 |
| `app-dir/interception-routes-multiple-catchall`                 | An interception route among several catch-alls     |
| `app-dir/mdx`                                                   | MDX pages, App and Pages Router — 27 cases        |
| `app-dir/metadata-icons-parallel-routes`                        | Icon metadata resolved through parallel routes    |
| `app-dir/navigation-focus`                                      | Focus placement after a client navigation         |
| `app-dir/parallel-routes-leaf-segments`                         | Parallel-route leaf segments, with no build error |
| `app-dir/segment-cache/prefetch-scheduling`                     | The order and priority of prefetch requests       |
| `define`                                                        | `next.config` `define` compile-time substitution  |
| `instrumentation-client-hook`                                   | `instrumentation-client.ts` in the browser — the slowest passing file, 677s |
| `link-on-navigate-prop`                                         | `<Link onNavigate>`                               |
| `opentelemetry/client-trace-metadata`                           | `clientTraceMetadata` propagated into the HTML    |
| `typescript-paths`                                              | `tsconfig` `paths` aliases, through a deployed build |
| `use-link-status`                                               | `useLinkStatus()`                                 |
| `use-router-with-rewrites`                                      | `useRouter()` in an app with `next.config` rewrites |
| `app-dir/_allow-underscored-root-directory`                      | An `_`-prefixed directory at the app root |
| `app-dir/action-forward-loop`                                    | A server action that forwards to itself |
| `app-dir/action-in-pages-router`                                 | A server action imported by a Pages Router page |
| `app-dir/actions-discarded-navigation-revert`                    | An action's result discarded by a navigation |
| `app-dir/app-esm-js`                                             | ESM `.js` sources in the App Router |
| `app-dir/app-fetch-deduping-errors`                              | Errors thrown by deduped `fetch` calls |
| `app-dir/app-new-scroll-handler-focus`                           | Scroll and focus handling on navigation |
| `app-dir/app-prefetch-false`                                     | `prefetch={false}` in the App Router |
| `app-dir/app-prefetch-static`                                    | Prefetch of a fully static route |
| `app-dir/app-root-params-getters/multiple-roots`                 | Root params across two root layouts |
| `app-dir/asset-prefix-absolute/asset-prefix-absolute-no-path`    | An absolute `assetPrefix` with no path — see defect 16 |
| `app-dir/autoscroll-with-css-modules`                            | Autoscroll with CSS modules loading late |
| `app-dir/back-forward-cache`                                     | Browser back/forward against the router cache |
| `app-dir/basepath-fallback-params`                               | PPR fallback params under a `basePath` |
| `app-dir/bfcache-regression`                                     | bfcache restore — passed on retry 1, see below |
| `app-dir/cache-components-bot-ua`                                | A bot user agent bypassing a `cacheComponents` shell |
| `app-dir/cache-components-errors/dev-cache-bypass`               | The non-dev half of the dev cache-bypass warning |
| `app-dir/cache-components-errors/unstable-deprecations`          | `unstable_cacheLife`/`cacheTag` deprecations |
| `app-dir/catchall-parallel-routes-group`                         | A catch-all inside a parallel-route group |
| `app-dir/client-module-with-package-type`                        | A client module in a `type: module` package |
| `app-dir/concurrent-navigations/mismatching-prefetch`            | A prefetch that no longer matches the navigation |
| `app-document/rendering`                                         | Pages Router `_document` rendering — 10 cases, see defect 17 |
| `legacy-link-behavior-pages`                                     | `legacyBehavior` on `<Link>` — 8 cases, see defect 17 |
| `new-link-behavior`                                              | The current `<Link>` behavior — 7 cases, see defect 17 |
| `next-head`                                                      | `next/head` tags in the document — see defect 17 |
| `next-image-legacy/default`                                      | `next/legacy/image` defaults — 28 cases, see defect 17 |
| `prerender-preview`                                              | Draft mode over a prerendered page — see defect 17 |
| `preview-fallback`                                               | Draft mode with a fallback page — see defect 17 |
| `rewrites-client-resolving`                                      | Client-side resolution of `next.config` rewrites — see defect 17 |
| `script-loader`                                                  | `next/script` strategies |
| `src-dir-support`                                                | An app whose sources live in `src/` |
| `trailing-slashes-href-resolving`                                | `href` resolution in a `trailingSlash` app |
| `app-dir/concurrent-navigations/server-patch-history`            | A server patch landing during a concurrent navigation |
| `app-dir/crypto-globally-available`                              | `crypto` reachable as a global in the Node runtime |
| `app-dir/csp-nonce-segment-scripts`                              | A CSP nonce applied to per-segment scripts |
| `app-dir/css-chunking`                                           | CSS split across chunks |
| `app-dir/css-client-side-nav-parallel-routes`                    | CSS loaded across a parallel-route navigation |
| `app-dir/css-media-query`                                        | A media query surviving the CSS pipeline |
| `app-dir/css-modules-pure-no-check`                              | CSS Modules with `pure` selector checks off |
| `app-dir/css-modules-rsc-postcss`                                | CSS Modules through PostCSS in a server component |
| `app-dir/css-modules-scoping`                                    | CSS Modules class scoping |
| `app-dir/cssnano-colormin`                                       | cssnano's `colormin` minification |
| `app-dir/dev-overlay/portal-not-affect-parent`                   | The dev overlay's portal, in a production build |
| `app-dir/disable-logging-route`                                  | `logging: false` in `next.config` |
| `app-dir/dynamic-import`                                         | `next/dynamic` in the App Router |
| `app-dir/ecmascript-features`                                    | Modern ECMAScript syntax through the build |
| `app-dir/error-boundary-navigation/override-node-env`            | An error boundary with `NODE_ENV` overridden |
| `app-dir/esm-client-module-without-exports`                      | A side-effect-only ESM client module |
| `app-dir/experimental-lightningcss-features`                     | Lightning CSS as the CSS transformer |
| `app-dir/fallback-prefetch`                                      | Prefetch of a fallback-rendered route |
| `app-dir/gesture-transitions`                                    | View transitions driven by a gesture |
| `app-dir/global-error/error-in-global-error`                     | An error thrown inside `global-error.tsx` itself |
| `app-dir/global-not-found/both-present`                          | `global-not-found` beside a route-level `not-found` |
| `app-dir/global-not-found/cache-components`                      | `global-not-found` under `cacheComponents` |
| `app-dir/global-not-found/css/global-not-found-css`              | CSS loaded by `global-not-found` |
| `app-dir/global-not-found/metadata`                              | Metadata emitted by `global-not-found` |
| `app-dir/global-not-found/no-root-layout`                        | `global-not-found` with no root layout |
| `app-dir/global-not-found/not-present`                           | The default 404 when no `global-not-found` exists |
| `app-dir/import`                                                 | Static and dynamic `import` forms |
| `app-dir/initial-css-not-found`                                  | CSS on the initial 404 response |
| `app-dir/initial-css-order`                                      | Stylesheet order in the initial HTML |
| `app-dir/interception-segments-two-levels-above`                 | An interception route reaching two segments up |
| `app-dir/mdx-font-preload`                                       | `next/font` preloads from an MDX page |
| `app-dir/mdx-no-mdx-components/mdx`                              | MDX with no `mdx-components.tsx` |
| `app-dir/metadata-dynamic-routes-async-deps`                     | Dynamic metadata routes awaiting async deps |
| `app-dir/metadata-non-standard-custom-routes`                    | Metadata routes at non-standard paths |
| `app-dir/metadata-route-like-pages`                              | Pages whose paths look like metadata routes |
| `app-dir/metadata-soft-nav-cache-components`                     | Metadata across a soft nav under `cacheComponents` |
| `app-dir/metadata-static-file/metadata-static-file-dynamic-route` | A static metadata file on a dynamic route |
| `app-dir/metadata-static-file/metadata-static-file-group-route`   | A static metadata file in a route group |
| `app-dir/metadata-static-file/metadata-static-file-intercepting-route` | A static metadata file on an interception route |
| `app-dir/metadata-static-file/metadata-static-file-parallel-route` | A static metadata file in a parallel route |
| `app-dir/metadata-static-file/metadata-static-file-static-route`  | A static metadata file on a static route |
| `app-dir/metadata-static-file-gsp`                               | A static metadata file beside `getStaticProps` |
| `app-dir/metadata-streaming/metadata-streaming-customized-rule`   | A custom rule for streamed metadata |
| `app-dir/metadata-streaming-parallel-routes`                     | Streamed metadata through parallel routes |
| `app-dir/metadata-thrown`                                        | An error thrown while generating metadata |
| `app-dir/mjs-as-extension`                                       | `.mjs` sources in the App Router |
| `app-dir/modal-catchall-sibling-dynamic-prefetch-loop`           | A modal catch-all prefetching its dynamic sibling |
| `app-dir/modularizeimports`                                     | `modularizeImports` rewriting barrel imports |
| `app-dir/monaco-editor`                                          | A large client-only dependency (Monaco) |
| `app-dir/multi-value-search-params-stale`                        | Repeated search-param keys against a stale cache |
| `app-dir/navigation-layout-suspense`                            | A layout suspending mid-navigation |
| `app-dir/navigation-with-queued-actions`                        | A navigation queued behind pending server actions |
| `app-dir/next-config`                                           | `next.config.js` options through a deployed build |
| `app-dir/next-config-ts/*`                                      | `next.config.ts` — 21 files, the whole CJS/ESM matrix (see below) |
| `app-dir/next-config-ts-native-mts/*`                            | `next.config.mts` under Node's native TS loader — 17 files (see below) |
| `async-modules`                                                 | Top-level `await` in a page module — see defect 19 |
| `i18n-support-catchall`                                         | A root catch-all in an `i18n` app — see defect 20 |
| `no-page-props`                                                 | A `getStaticProps` page whose data route has no rule — see defect 21 |
| `app-dir/asset-prefix-absolute`                                  | An absolute `assetPrefix` carrying a path — see defect 22 |

The two `next-config-ts` rows are the only collapsed entries in this table. They
are a 38-file matrix of the same fixture — a `next.config` written in TypeScript,
crossed with CJS/ESM, `export default`/`export as default`, path aliases,
`tsconfig` `extends`, JSON imports, node builtins and top-level `await` — and
every file passed on attempt 0 in 96–99s. The individual paths are in the
manifest's `rules.include`; spelling all 38 out here would say nothing the two
rows don't. They are worth running despite the redundancy because each one is a
distinct `next build` whose resolved config the adapter has to read back out of
`required-server-files.json`.

Two rows here did not pass on attempt 0. `router-autoscroll` failed once at 323s
with the fixture's own scroll assertions and passed on retry 1, with no cdk-nextjs
error in either; `bfcache-regression` did the same. Both are listed because the
harness runs with `--retries 1` and treats a retry pass as a pass, but they are the
files to suspect first if a scheduled run goes red for no other reason.

`encoded-slash-params` passing is worth noting next to the `prerender-encoding`
row below: `%2F` in a param works, which is part of why that file turned out not
to be an encoding bug at all.

## Passing in part — in `suites`

A file listed in the manifest's `suites` is **included**, with the cases named in
its `failed` array skipped. next.js's own filter does this
(`test/get-test-filter.js` turns them into `excludedCases`, which `run-tests.js`
passes as a negative `--testNamePattern`), so the case names have to be the full
jest names, `describe` title included. Use this instead of dropping a whole file
when the residual failures have a verdict below.

| File                         | Cases | Skipped, and why                                                     |
| ---------------------------- | ----- | -------------------------------------------------------------------- |
| `app-dir/trailingslash`      | 6 / 8 | The 2 `should revalidate a page with generated static params` cases — CDN-inherent, see below |
| `app-dir/resume-data-cache`  | 3 / 5 | The 2 `should have consistent data between static and dynamic renders` cases — architectural, see below |
| `dynamic-route-interpolation` | 3 / 7 | The 4 cases requesting unencoded `[`/`]` in the path — CDN-inherent, see below |

## Fixed — real bugs, found by the harness

### 1. Every POST with a body was rejected at the edge — `403 InvalidSignatureException`

**Files:** `app-dir/actions-streaming`,
`app-dir/dynamic-interception-route-revalidate`, `app-dir/app-basepath`.
**Verdict: fixed.** All three are now in `rules.include`.

Every case that drove a server action and then expected the client to act on the
response failed; everything else in the same files passed. The server function's
own logs were clean — ~60–80ms per invocation, no error — which read at first as
"the reply is being made and something eats it on the way back".

The Playwright traces (`test/traces/`, written because `run-tests.js` sets
`TRACE_PLAYWRIGHT=true`) said otherwise. Every action POST came back:

```
POST /base/client  →  403
x-amzn-errortype: InvalidSignatureException
{"message":"The request signature we calculated does not match the signature you provided. …"}
```

Reproduced by hand against a deployment, and the boundary is exactly the body:

```
POST, 2-byte body                  → 403 InvalidSignatureException
POST, no body                      → 200
POST, 2-byte body + correct
  x-amz-content-sha256 header      → 200
```

That is [documented AWS behavior][oac]: *"If you use PUT or POST methods with
your Lambda function URL, your users must compute the SHA256 of the body and
include the payload hash value of the request body in the
`x-amz-content-sha256` header when sending the request to CloudFront. Lambda
doesn't support unsigned payloads."* CloudFront's origin access control signs the
origin request with SigV4 and supplies the empty-body hash for a GET, but it will
not hash a body — so the viewer has to, and a browser has no way to do that on
its own.

cdk-nextjs already solves this. `src/nextjs-build/patch-fetch.js` wraps
`fetch`/`XMLHttpRequest` to compute the hash, and
`NextjsBuild#patchFetchInClientJs` prepends it to the client entrypoint chunks —
for `NextjsGlobalFunctions` only, because it is the only type fronted by a signed
Function URL.

The bug was **where that call lived**: inside `runNextBuild()`, so
`skipBuild: true` skipped it along with the build. The harness sets
`skipBuild: true` (it has to — `scripts/e2e-deploy.sh` runs `next build` itself,
to emit the markers the harness parses), so every harness deployment shipped an
unpatched client. So did every user's, with the same prop.

Fix: the call moved out of `runNextBuild()` into the constructor, after the build
gate, still guarded on `NextjsType.GLOBAL_FUNCTIONS`. Since the same `.next` can
now be synthesized more than once, the prepend is guarded by a marker comment so
it cannot double-wrap.

Two things this says about the test setup, beyond the fix:

- `examples/e2e-tests` covers server actions and is green on
  `NextjsGlobalFunctions`, but only ever on the `skipBuild: false` path. It could
  not have caught this.
- The harness is the regression test for it. There is no `NextjsBuild` construct
  test to add this to — one would need a whole synthetic `.next` (adapter
  manifest, `BUILD_ID`, client chunks) — and the harness exercises exactly the
  `skipBuild: true` path that broke.

### 2. Seeded prerender headers mislabeled every RSC response as HTML

**Files:** `app-dir/app-basepath` (the 3 action-`redirect()` cases),
`app-dir/segment-cache/deployment-skew`,
`app-dir/static-rsc-cache-components`. **Verdict: fixed**
(`appPageCacheHeaders` in `src/adapter/cache-utils.ts`). All three now pass.

`onBuildComplete` seeded the HTML prerender's `fallback.initialHeaders` verbatim
into the `APP_PAGE` cache entry — including `content-type: text/html;
charset=utf-8`. That is a category error: `initialHeaders` describes how a
platform should serve the prerendered *file* off a CDN, whereas a cache entry is
served by `app-page-runtime.js`, which `appendHeader`s `cachedData.headers` onto
the response and *only then* picks the HTML or the flight variant the request
actually asked for. And `send-payload.js` will not correct it:

```js
if (!res.getHeader('Content-Type') && result.contentType) {
  res.setHeader('Content-Type', result.contentType)
}
```

So every RSC request to every prerendered page answered the flight payload
labeled `text/html`, with a doubled `vary` and `x-nextjs-prerender` for good
measure. Three unrelated-looking failures were all this:

- `deployment-skew`'s `header is set on RSC responses` asserted
  `content-type: text/x-component` and got `text/html; charset=utf-8`.
- `static-rsc-cache-components` measured a navigation duration of `NaN` — the
  navigation never completed, because the client router rejects a flight payload
  that arrives as HTML.
- `app-basepath`'s action `redirect()` cases answered with *no head at all*.
  `createRedirectRenderResult` (`packages/next/src/server/app-render/
  action-handler.ts`) streams an app-relative redirect by fetching the target
  back through the deployment's own origin and gating on
  `response.headers.get('content-type')?.startsWith(RSC_CONTENT_TYPE_HEADER)`.
  Mismatch → `response.body?.cancel()` → `RenderResult.EMPTY` → a zero-byte
  streamed Lambda response, which is defect 3.

Fix: filter `content-type`, `vary`, `x-nextjs-prerender` and
`x-nextjs-postponed` out of `APP_PAGE` entries, keeping everything the render
would genuinely have stored (`x-nextjs-stale-time`, `x-next-cache-tags`, and
whatever the app set through `headers()`/`cookies()`). `APP_ROUTE` entries are
deliberately *not* filtered — a route handler's `content-type` is part of its
cached response, and `app-route.js` replays those headers verbatim. Regression
test: `src/adapter/cache-utils.test.ts`.

### 3. A zero-byte streamed response lost its entire head on a Function URL

**Files:** `app-dir/app-basepath` (same 3 cases, as the downstream half of
defect 2). **Verdict: fixed** (`ResponseSink.padEmptyBody`, set on
`LambdaResponseSink`).

Independent of *why* a response ends up empty, an empty one was unserveable. A
`RESPONSE_STREAM` Lambda writes an `awslambda.HttpResponseStream.from` prelude,
a `\0`×8 delimiter, then the body. With zero payload bytes after the delimiter:

| Integration    | Result                                               |
| -------------- | ---------------------------------------------------- |
| Function URL   | prelude discarded — bare `200 application/octet-stream`, stream not closed cleanly |
| API Gateway    | `502`                                                |

Writing a single space byte fixes both, and costs nothing that matters: a `304`
and a `HEAD` have no body by definition, and the integration overwrites
`content-length` itself. Verified by instrumenting the deployed function rather
than by deduction — three plausible guesses (a head event racing `pipeToSink`,
`useDefineForClassFields` breaking `res.flush`, the sub-fetch's
`content-encoding`) were all wrong, and the `[probe]` logs settled it in one
run. Regression tests: `src/runtime/http/sink.test.ts`.

### 4. The home page's cache entry had no flight payload — every navigation to `/` 404'd

**Files:** `app-dir/segment-cache/cached-navigations` (3 of 14 cases).
**Verdict: fixed** (`groupPrerenders` in `src/adapter/cache-utils.ts`).

Next.js emits up to three outputs per prerendered route — `/blog/hello`,
`/blog/hello.rsc`, `/blog/hello.segments/*.segment.rsc` — and `onBuildComplete`
has to group them by route before it can seed one cache entry. It did that by
stripping suffixes, then reconstructing `${route}.rsc` to find the payload.

The root route breaks that, because it cannot be named `/.rsc`. Next emits its
HTML at `/` but its payload at `/index.rsc` and its segments under
`/index.segments/` — and with a `basePath`, `/prod` versus `/prod/index.rsc`.
Measured over both fixture shapes:

```
no basePath:   "/"      html=yes rsc=no  segments=4
basePath /prod: "/prod"       html=yes rsc=no  segments=0
                "/prod/index" html=no  rsc=yes segments=4   ← skipped, no HTML
```

So the home page was seeded with `html` but `rscData: undefined`. `app-page-
runtime.js` handles that case by falling back to `cachedData.html.contentType`,
and under `cacheComponents` sends `res.statusCode = 404` with
`RenderResult.EMPTY` — which is exactly what the harness recorded:

```
GET /?_rsc=_aiG6yzaaivOHhuC
  → 404, content-type: application/json, x-nextjs-cache: HIT, empty body
```

Fix: group by parsing the emitted names rather than reconstructing them, and
remap a trailing `/index` onto its parent when — and only when — the parent is
itself a prerendered HTML route. The condition matters: an app with a real
`app/index/page.tsx` has a genuine `/index` route that must keep its own group.
Regression test: `src/adapter/cache-utils.test.ts`.

### 5. A param whose name prefixes another param's got the wrong value

**Files:** `app-dir/use-params` (2 of 3 cases).
**Verdict: fixed** (`repairRouteParamQuery` in `src/runtime/dispatch.ts`, commit
`acaf372`).

The fixture routes `app/[id]/[id2]/page.tsx`. `@next/routing` expands a matched
route's query by substituting group names into `$nxtPid`-style placeholders in
insertion order, so `$nxtPid2` is rewritten by the `nxtPid` substitution first
and keeps a literal `2`: the route was invoked with `nxtPid2 = "a2"` where `a`
was the *first* param's value. Any two params where one name is a prefix of the
other hit it — `[id]/[id2]`, `[slug]/[slugPart]`.

Fix: after resolution, rebuild the param query from `routeMatches`, which
`@next/routing` reports unmangled. Regression test in `src/runtime/dispatch.test.ts`
renames a param pair in the committed `app-playground` capture, because no
committed fixture has a naturally prefixing pair.

### 6. A `trailingSlash` app's canonical URLs all 404'd

**Files:** `app-dir/trailingslash` (6 of 8 cases).
**Verdict: fixed** (`withTrailingSlashVariants` and `Dispatcher.normalizePathname`
in `src/runtime/dispatch.ts`, commit `9171856`).

With `trailingSlash: true`, `/a/` is the URL Next.js's own 308 sends browsers to
— but the build's output pathnames never carry the slash, and `@next/routing`
matches pathnames by exact equality. So every canonical URL missed, including
`fetch('/api/revalidate')`, which got the 404 page rather than the route handler.

Fix: register the slash-bearing variant of each pathname, and normalize it away
again on the way out so the render lands on its prerender's cache key. Extension-
bearing static files are deliberately excluded — Next.js compiles the *opposite*
redirect for those, and shadowing it would serve `/x.js/`.

### 7. The build-time cache handler was write-only, which `cacheComponents` cannot survive

**Files:** `app-dir/resume-data-cache` — `next build` itself failed, so all 5
cases were unrunnable.
**Verdict: fixed** (`LocalFileCacheHandler.get` in
`src/adapter/local-file-cache-handler.ts`, commit `65ec780`).

```
Error: Route "/": Next.js encountered uncached or runtime data during prerendering.
```

Under `cacheComponents` a prerender runs twice: a first pass that fills caches
(every `fetch` misses, runs for real, and is `set`), then a final pass in which
every await must settle in a microtask — anything still pending is by definition
runtime data. The build-time handler only wrote, so the final pass re-issued the
`force-cache` fetch and Next.js aborted the page. Next.js's own `FileSystemCache`
reads back for exactly this reason.

Isolated on published next 16.3.5 — each row a separate build of the same
fixture:

| Config                                          | Before | After |
| ----------------------------------------------- | ------ | ----- |
| `cacheComponents: true` alone                   | builds | builds |
| `+ cacheHandler`                                | fails  | builds |
| `+ cacheHandler + NEXT_ADAPTER_PATH`            | fails  | builds |

Regression test: `src/adapter/cache-handler.test.ts` covers both the in-process
read-back and a second instance reading the file, because `next build` renders
pages in worker processes.

### 8. `revalidateTag` could not reach a single build-time prerender

**Files:** `app-dir/trailingslash` (2 of 8), `app-dir/segment-cache/prefetch-inlining`
(1 of 15), `app-dir/resume-data-cache` (2 of 5) — all three failing on one cause.
**Verdict: fixed** (`entryTags` and `checkIfRevalidated` in
`src/adapter/s3-cache-handler.ts`, commit `47354d2`).

Next.js derives a stored entry's tags from its own `x-next-cache-tags` header.
The handler instead read a `tags` array that only a runtime `set` writes, and a
build-time prerender is not written by `set` at all — the adapter's
`onBuildComplete` writes those entries as files. So a prerendered page carried no
tags the handler could see, `revalidateTag` had nothing to match it against, and
an app whose pages are all static never revalidated: it served the build's
output until something happened to re-render the page at runtime.

Two bugs met here. The second was that `storeDynamoDBTagMappings` stamped
`revalidatedAt = now` on the mapping row it wrote *while storing* an entry — a
timestamp necessarily later than the entry's own `lastModified` — so every tagged
entry looked revalidated one millisecond after it was stored and was deleted on
the next read. Mapping rows now record only `createdAt`, and the revalidation
timestamp lives on a per-tag marker row (`sk` = the bare tag) that
`checkIfRevalidated` reads by primary key.

Ruled out along the way, recorded so they are not re-investigated: the resume
data cache *is* seeded (it rides inside `postponed`), `segmentData` round-trips
with its `/_tree`, `/_full` and `/__PAGE__` keys intact, and a seeded entry's
missing `cacheControl` is harmless because Next.js takes that from the prerender
manifest.

### 9. On-demand revalidation left the CDN copy of a prerender untouched

**Files:** `app-dir/segment-cache/prefetch-inlining` (1 case),
`app-dir/trailingslash` (2 cases).
**Verdict: fixed** (`implicitTagPath`/`invalidationVariants` in
`src/adapter/s3-cache-handler.ts`, `seedTagMappings` in
`src/lambdas/post-deploy/`). `prefetch-inlining` went 14/15 → **15/15 on the
first attempt**, so the file is now in `rules.include`. `trailingslash`'s 2 cases
stay red on timing alone — they re-request inside 3s, see "Revalidation
re-requested about a second later" below.

Defect 8 fixed the origin: after a `revalidatePath` the Lambda does re-render.
The browser never saw it, because a prerendered response is served
`cache-control: s-maxage=31536000` and CloudFront answered from the edge.
`revalidateTag` did create an invalidation, but only for paths it could derive
from the tag's DynamoDB mapping rows — and a build-time prerender has none, for
exactly the reason defect 8 turned on. Measured directly:

```
$ curl -sI 'https://…/?_rsc=probe' -H 'RSC: 1' -H 'Next-Router-Prefetch: 1' \
    -H 'Next-Router-Segment-Prefetch: /__PAGE__'
cache-control: s-maxage=31536000
x-cache: Miss from cloudfront      # and `Hit from cloudfront` on the second call
```

Three parts to the fix:

- A `revalidatePath` names its path in the tag itself (`_N_T_/blog`), so the
  invalidation no longer needs a mapping row for the commonest case.
- Each path is invalidated in all four forms it can be cached under — with and
  without a trailing slash, each with and without a query string. An invalidation
  path matches only the query string it spells out, and a page's RSC payload is
  cached under `?_rsc=<hash>`; dropping the HTML while leaving the payload behind
  leaves the router navigating to the pre-revalidation page.
- The adapter writes a tag manifest into the init cache and the post-deploy
  custom resource turns it into the mapping rows a runtime `set` would have
  written, which is what a `revalidateTag("posts")` on a static page needs. The
  custom resource now also runs after the init cache upload, which
  CloudFormation does not order on its own.

Only the first two parts are under harness coverage. The harness reuses one
deployment and updates Lambda code by hotswap, which never runs CloudFormation,
so the post-deploy custom resource does not fire between fixtures and the seeded
mapping rows are never exercised there — `prefetch-inlining` goes green on the
implicit-tag path alone. `seedTagMappings` is covered by its unit test
(`src/lambdas/post-deploy/seed-tag-mappings.test.ts`) and by a full deploy.

### 10. Every fixture shared one cache namespace, and served each other's pages

**Files:** `app-dir/error-boundary-navigation` (5 of 7),
`app-dir/metadata-navigation` (1 of 7). **Verdict: fixed** (`getBuildId` in
`src/nextjs-build/nextjs-build.ts`). Both now pass 7 of 7.

Two fixtures in a row, and the second answered a missing path with the *first*
fixture's `/_not-found` page. Nothing in either app explained it, and the failing
assertions were about content that belonged to a different test file entirely.

The cause is upstream and deliberate. `next/dist/build/index.js`'s `getBuildId()`:

```js
if (config.deploymentId) {
  // Set a constant but "random" string because various tools perform
  // `.replace(escapedBuildId, ....)`
  return 'build-TfctsWXpff2fKS'
}
return nanoid()
```

`config.deploymentId` comes from `NEXT_DEPLOYMENT_ID`
(`packages/next/src/server/config.ts`), which `scripts/e2e-deploy.sh` sets per
fixture so that skew protection has something to check. So `.next/BUILD_ID` was
the *same 21-character constant* for every fixture the harness ever built.

cdk-nextjs uses `buildId` for exactly one thing: partitioning the cache. The S3
prefix, the DynamoDB partition key, the static assets' `BUILD_ID` metadata, and
post-deploy's pruning and seeding all key off it. One constant meant one
namespace, so fixture *n+1* read fixture *n*'s prerenders straight out of S3.

Fixed by suffixing the deployment id when the app sets one. Nothing constrains the
value — the `/_next/data/<buildId>/…` URL space is matched with a literal path
parameter and the runtime's own `manifest.buildId` comes from the adapter build
context — so a longer id is free. Confirmed in the bucket afterwards: one prefix
per fixture rather than one shared `build-TfctsWXpff2fKS/`.

This is only reachable with `NEXT_DEPLOYMENT_ID` set, which is the documented way
to turn on skew protection, so it was a real user-facing bug and not a harness
artifact: two apps in one account that both enabled skew protection would have
collided.

### 11. An unmatched optional catchall rendered a segment nobody asked for

**File:** `app-dir/layout-params` (1 of 6). **Verdict: fixed**
(`repairRouteParamQuery` in `src/runtime/dispatch.ts`), unit-tested in
`dispatch.test.ts`.

`#lvl2-layout > div` was asserted empty and had one child: a layout rendered a
route segment the request never contained.

`@next/routing` reports an unmatched optional catchall as an *unfilled* param
rather than an absent one. Measured directly out of a running shell —
`resolveRoutes` for `/optional-catchall` against
`^[/]?/optional\-catchall(?:/(?<nxtPparams>.+?))?(?:/)?$`:

```
resolvedPathname: /optional-catchall/[[...params]]
resolvedQuery:    { nxtPparams: undefined }   // the key is present, with no value
routeMatches:     { nxtPparams: undefined }   // so is this one
```

Both maps are typed `Record<string, string>` / `ResolveRoutesQuery`, neither of
which admits `undefined`, and `JSON.stringify` hides the key entirely — which is
why the first attempt at this fix looked for `""` and missed. `RouteModule.prepare`
invents a param for every `nxtP*` key it finds, so the layout maps over a
one-element array; `next start` has no `params` key at all there. The value the
segment rendered was the string `"undefined"`, because `formatTarget` builds the
invocation URL through `URLSearchParams.append`, which stringifies.

`repairRouteParamQuery` already existed to correct the opposite shape (defect 5: a
param whose name prefixes another's, where the query and `routeMatches` disagree),
so the fix is a second pass over the same map that deletes any `nxtP*` key neither
side filled. It goes whether or not `routeMatches` echoes the key: no route param
can legitimately be unfilled — a required segment captures `[^/]+?` and a required
catchall `.+?`.

Verified offline rather than by a deploy: the fixture was built into
`next.js/lp-app` and served by both `next start` and our container shell on the
same build, which is what made the `"undefined"` visible. All six of the file's
assertions match `next start` after the fix.

### 12. `robots.txt` and `sitemap.xml` went out as `application/octet-stream`

**File:** `app-dir/use-cache-metadata-route-handler` (3 of 10).
**Verdict: fixed** (`setBodyFileContentType` in `src/runtime/static-files.ts`).

Three cases, one shape — the body was right and the type was wrong:

```
/sitemap.xml          → application/octet-stream  (expected application/xml)
/robots.txt           → application/octet-stream  (expected text/plain)
/manifest.webmanifest → application/octet-stream  (expected application/manifest+json)
```

Probed live to confirm, and the response heads named the mechanism:
`accept-ranges: bytes`, `last-modified: Tue, 01 Jan 1980`, `etag: W/"14-…"`. That
is `send` serving a file off disk — which is `serveStatic`, which is how the
runtime answers a `static-file` dispatch — and `send` types a response from the
file's own extension. A static metadata route is staged as `<route>.body`, and
`.body` is not a media type.

`/products/sitemap/0.xml` passed in the same run (`x-nextjs-cache: HIT`,
`application/xml`), which is the tell: a `generateSitemaps` route goes through the
cache and carries its headers, while the root-level ones are static files.

The type is dropped by next.js's adapter API, not by us.
`packages/next/src/build/adapter/build-complete.ts` routes these through
`isStaticMetadataFile()` and pushes a bare `STATIC_FILE` — `id`, `pathname`,
`filePath`, nothing else — while the headers the route handler actually set sit
unreferenced in a sibling `<route>.meta` that is never staged. `next start` reads
that file through the response cache and answers `text/plain`.

Fixed by stripping `.body` and typing from the route's own extension, which for
this population is the same answer: the set is closed (`favicon.ico`, `icon.*`,
`apple-icon.*`, `opengraph-image.*`, `twitter-image.*`, `sitemap.xml`,
`robots.txt`, `manifest.{json,webmanifest}`) and every member's type is implied by
its extension. `send` skips its own detection when `Content-Type` is already set,
so nothing has to be threaded through its options. `favicon.ico` had the same bug
and nobody noticed, because browsers sniff icons.

### 13. A root-params app 404'd at every URL, because its prerenders were draft-gated

**File:** `app-dir/parallel-routes-root-param-dynamic-child` (10 of 14).
**Verdict: fixed** (`addPrerenderPathnames` in `src/adapter/build-outputs.ts`).

Ten cases died on `page.waitForSelector('#reveal')` timing out, which reads like a
hydration failure — `#reveal` is a checkbox in a client component. It was not. A
live probe found the page itself missing:

```
GET /en   → HTTP/2 404, x-nextjs-prerender: 1, x-nextjs-cache: MISS
            (all 7 script srcs and the preload href return 200)
```

The fixture is a **root params** app: `app/[locale]/page.tsx` with
`generateStaticParams()` and deliberately no `app/layout.tsx`. Its params can
never be filled from a request, because there is no static ancestor to route
through — so next.js emits the `dynamicRoutes` rule for it *gated on draft mode*:

```json
{ "sourceRegex": "^/(?<nxtPlocale>[^/]+?)(?:/)?$",
  "has": [{ "type": "cookie", "key": "__prerender_bypass" },
          { "type": "cookie", "key": "__next_preview_data" }] }
```

That is next.js saying: invoke the function only for a draft-mode request, and
otherwise serve the prerender you were handed. Vercel's CDN does exactly that.
Our manifest listed no `/en`, so dispatch matched nothing and answered the
prerendered 404 — for `/en`, `/en.rsc` and every `.segments/…` prefetch alike,
which is why the client never got the layout's bundle.

Fixed by registering a *concrete* prerender pathname as an entrypoint when a gated
dynamic rule matches it and no ungated one does, owned by the entrypoint of
`prerender.route` (preferring the route's `.rsc` entrypoint for an `.rsc`
pathname). The narrowing matters: registering every concrete prerender adds inert
keys — `/index.segments/*.segment.rsc` for an app whose `/` is static, which
`resolveRoutes` never resolves to — and a route with an ungated rule already
reaches its template, where `RouteModule.prepare` re-derives the params from the
pathname. Unknown params keep 404ing, as `next start` does for `/xx`.

Verified at the dispatch level against the real fixture's captured build context
(`/en`, `/fr`, `/en.rsc`, `/en/gsp/stories/static-123` all resolve; `/xx` still
404s; `/en/no-gsp/stories/1` still resolves to the template). End-to-end
verification needs a deployment: offline the runtime cache reads S3 only, so a
fully-prerendered route answers `invariant: cache entry required but not
generated`.

### 14. Every PPR fallback shell was re-rendered per request instead of resumed

**File:** `app-dir/sub-shell-generation` (6 of 7).
**Verdict: fixed** (the prerender-group loop in `src/adapter/adapter.mts`).

Six cases, one diff — the shell was right, the sentinel was wrong:

```
- "rootLayout": "Root Layout: (buildtime)"     ← next start
+ "rootLayout": "Root Layout: (runtime)"       ← ours
```

The fixture's root layout is `'use cache'`, so `(buildtime)` is the assertion that
the response *resumed* a shell built at build time. The seventh case, `/fr/1`, is
fully prerendered and passed: only the three fallback shells (`/[lang]/[slug]`,
`/en/[slug]`, `/fr/[slug]`) failed.

A dynamic route template is not a dead cache entry. With PPR it *is* the route's
fallback shell, and the server looks it up under that literal key:
`app-page-runtime` reads `prerenderManifest.dynamicRoutes[route].fallback` — the
template string, verbatim — and calls
`routeModule.handleResponse({ cacheKey, isFallback: true })`. The Pages Router does
the same with `srcPage` for an ISR fallback. Our seeding loop skipped every group
whose pathname contained `[`, commented "they don't have actual content", so every
one of those lookups missed and the shell was rendered per request.

They do have content. The fixture's `.next/server/app/[lang]/[slug].meta` carries a
2,630-byte `postponed` state, and after the fix the seed directory gains the three
entries with the shells the test asks for:

```
[lang]/[slug].json   html 1004B  postponed 2630B  segs 2  root-layout: buildtime, lang-layout deferred
en/[slug].json       html 1178B  postponed 3388B  segs 2  root-layout: buildtime, lang-layout: buildtime
fr/[slug].json       html 1178B  postponed 3363B  segs 2  root-layout: buildtime, lang-layout: buildtime
```

Which is exactly the per-URL expectation table in the fixture: `/es/1` gets the
`[lang]` shell (lang layout deferred to the resume → `(runtime)`), `/en/1` and
`/fr/2` get their locale's shell (`(buildtime)`).

Nothing extra was needed to keep non-PPR templates out: a route with no shell emits
no prerender output at all, and a Pages Router template gets no kind from
`getRouteToCacheKindMap` and is skipped one line later. Measured with
`scripts/e2e-offline.sh app-dir/sub-shell-generation`, which is also how the seed
directory above was read; `adapter.mts` has no jest coverage, so the fixture build
is the evidence.

### 15. A Pages Router app's custom `pages/404` was replaced by the built-in one

`test/e2e/404-page-app`, both cases, on both attempts. The fixture renders
`<h1 id="404-title">{props.extraProp}</h1>` from `pages/404.js` and supplies
`extraProp: 'Hi There'` from `pages/_app.js`'s `getInitialProps`. We answered
`/abc` with a 404 whose title was `404: This page could not be found` — the
built-in page — so `$('#404-title').text()` was `""`.

**Verdict: fixed.** `resolveNotFoundTarget` in `src/runtime/dispatch.ts` looked for
`/_not-found`, then `/_error`, and only then a *static* `/404` file. next itself
looks for `/_not-found`, then `/404`, then `/_error` —
`base-server.ts`'s `renderErrorToResponse`, `if (!result && (await this.hasPage('/404')))`
sitting between the app-dir branch and the `statusPage` fallback. The order
matters because `/404` is the app's own page and `/_error` is the framework's.

The static-file fallback was not wrong, just incomplete: `pages/404.js` is normally
prerendered to HTML and does reach us as a static file. It stays *invocable* when
something forces a per-request render, and `pages/_app.js` having `getInitialProps`
is exactly that — it disables automatic static optimization for every page in the
app, which is what the fixture is built to exercise. So the entrypoint existed all
along (`entrypoints: ["/_error", "/404", "/err", "/index"]`) and nothing looked at
it.

Offline, `scripts/e2e-offline.sh 404-page-app`, before and after:

```
before   status 404   <title>404: This page could not be found</title>
after    status 404   <h1 id="404-title">Hi There</h1>
next start            <h1 id="404-title">Hi There</h1>
```

Covered by `dispatch.test.ts`'s "prefers an invocable custom /404 over /\_error",
which also pins `/_not-found` still winning when an app has all three. Verified
against a deployment too — the file ran green in 163.7s on retry 0 — and joins
`rules.include` with the rest of its batch.

### 16. A path-style `assetPrefix` 404'd every bundle

`app-dir/asset-prefix` (2 of 7 cases, reproducible across two attempts), and by
inspection `app-dir/asset-prefix-with-basepath` (7 cases) the same way. Both came
back green from batch 10 and are in `rules.include`.

Not a routing bug, which is where the stack traces first pointed. jest
misattributes the failure because the fixture's bundle assertions live in an
un-awaited `bundles.forEach(async …)`, so the rejection is reported against
whichever test was running; every trace resolves to the `forEach` line. Running
the full runtime locally against a hand-assembled deployment root returns
`200 {"message":"test"}` for `/api/test-json`, `/not-custom-asset-prefix/api/test-json`
and `/custom-asset-prefix/api/test-json`. Routing is fine.

What failed is the prefixed bundle URL. With `assetPrefix: '/custom-asset-prefix'`,
Next.js emits script `src`es under that prefix while the build's `staticFiles`
keys stay unprefixed (`/_next/static/…`), and the Lambda package contains no
`.next/static` directory at all — the static assets live in S3 behind
CloudFront's `_next/static*` behavior. So `/custom-asset-prefix/_next/static/…`
missed that behavior, fell through to the default behavior, reached the compute
origin, and 404'd because the file was never packaged.

**Verdict: fixed**, for the two Global `NextjsType`s. The awkward part is that
Next.js applies `assetPrefix` *on top of* `basePath` rather than under it: a
`basePath: '/custom-base-path'` app with `assetPrefix: '/custom-asset-prefix'`
emits `/custom-asset-prefix/_next/static/…` while its objects are keyed
`custom-base-path/_next/static/…`. So the prefix cannot simply be joined, and an
S3 origin keys on the request URI (`originPath` can only prepend), which leaves a
viewer-request function as the only place to map one onto the other:

- `readNextConfigAssetPrefix` (`src/utils/base-path.ts`) reads `assetPrefix` out of
  the same `required-server-files.json` that `basePath` comes from, reducing an
  absolute or protocol-relative prefix to `""` — that names an origin cdk-nextjs
  does not serve, so there is nothing to add.
- `NextjsBuild.nextConfigAssetPrefix` exposes it; both Global root constructs pass
  it to `NextjsDistribution`.
- `NextjsDistribution.addAssetPrefixBehavior` adds `<assetPrefix>/_next/static*`
  against the static origin, with a CloudFront Function that replaces the prefix
  with `basePath`. It counts against the 25-behavior budget, which the error
  message now names.

Covered by four `nextjs-distribution.test.ts` cases (the behavior and its rewrite,
the `basePath` variant, the four prefixes that need no behavior, and the budget)
and a `readNextConfigAssetPrefix` block in `base-path.test.ts`. The regional
`NextjsType`s cannot serve a path-style prefix at all — API Gateway's
`_next/static` resource and a container's own files both sit at the unprefixed
path — so `NextjsBaseConstruct.warnUnservedAssetPrefix` warns at synth instead of
deploying something that 404s. Documented in `README.md` under
"`next.config.js` options cdk-nextjs reads", which it was not before.

The two fixture files confirmed the fix on the batch-10 deployment: 7 of 7 and 7
of 7, both on attempt 0.

### 17. Every Pages Router app's home page 404'd

Eleven files across two batches, all with different-looking assertions and one
cause. From batch 9: `new-link-behavior` (2 of 7),
`legacy-link-behavior-pages` (2 of 8), `prerender-preview` (1 of 9),
`preview-fallback` (1 of 6), `app-document/rendering` (1 of 10) and
`next-image-legacy/default` (1 of 28). From batch 10, run before the fix was
bundled: `next-head` (4 of 5), `next-image-legacy/unicode` (5 of 5),
`no-page-props` (2 of 5), `rewrites-client-resolving` (5 of 5) and
`async-modules` (2 of its 3 failures).

They read as unrelated: an empty `<a>` text, `JSON.parse` on an empty string, a
`#css-in-cjs-count` of `0` where `2` was expected, a `naturalWidth` of `null`, an
`#app-value` of `""`. What they have in common is the URL — every one of those
cases requests `/`, and in each file the cases that request anything else passed.
`/` was returning the built-in 404 page — which is still a Next.js document, so it
hydrates and the browser logs nothing but a single "Failed to load resource: 404".
Batch 10 made that unmistakable: `next-head` asserted on the whole `<head>` and
printed the 404 document's `<title>` in the diff, and the four files whose every
case starts at `/` failed 5 of 5, 5 of 5, 4 of 5 and 2 of 5.

`/` was not a route we knew about. The adapter hook derives every Pages Router
pathname with `normalizePagePath(page)`, and `normalizePagePath("/")` is `"/index"`
(`next/dist/shared/lib/page-path/normalize-page-path.js`). So the home page arrives
from `next build` as `pathname: "/index"` — as a `PAGES` output for an SSG or SSR
home page (`build-complete.ts`, `pathname: route`), and as a `STATIC_FILE` for a
fully-static one (same file, the `staticPages.has(page)` branch). Nothing else in
the outputs carries `/`: automatic static optimization means a static home page has
no `PAGES` output at all. `manifest.pathnames` is the union of the entrypoint and
static-file keys, `resolveRoutes` can only resolve a pathname that is in it, so `/`
resolved to nothing and dispatch fell through to the 404 ladder.

**Verdict: fixed.** `routableStaticPathnames` → `routablePathnames`
(`src/adapter/build-outputs.ts`) maps a reported `${basePath}/index` to
`basePath || "/"`, and both `collectStaticFiles` and `addEntrypoint` register the
real pathname alongside the reported one. `/index` is kept because next's own
minimal mode — the mode the runtime runs in — rewrites `req.url` and
`x-matched-path` from `/index` to `/` before matching (`base-server.ts`, "in
minimal mode"), so dropping it would be a divergence in the other direction.

The exact match is what makes this safe. `normalizePagePath("/index")` is
`"/index/index"`, so a reported `/index` can only have come from the page `/`; the
same page's data route `/_next/data/<buildId>/index.json` and an App Router
`/index.rsc` are real URLs and are untouched. App Router was never affected — it
reports its home page as `/` already, which is why the first 72 green files passed
with this bug in place. Three cases in `build-outputs.test.ts` cover the static
home page, the invocable one, and the `basePath` form (`/prod`, not `/prod/`).

Why it took nine batches to find: every fixture whose home page is App Router, or
whose tests never fetch `/`, is unaffected, and the first six that did fetch it
each reported a different symptom several layers from the cause.

**Confirmed on a deployment.** Batch 11 re-ran all eleven against the bundled fix.
Eight came back green and are in `rules.include`: `new-link-behavior`,
`legacy-link-behavior-pages`, `prerender-preview`, `preview-fallback`,
`app-document/rendering`, `next-image-legacy/default`, `next-head` and
`rewrites-client-resolving`. Of the other three, `next-image-legacy/unicode` never
deployed at all (defect 18, below), and `async-modules` and `no-page-props` each
have one remaining case with a different cause — both awaiting a verdict below.

### 18. A space in a `public/` filename failed the whole synth

`next-image-legacy/unicode` (5 of 5, all four attempts across two batches). Not an
assertion failure: `cdk deploy` never ran. Every attempt ended in

```
Error: Invalid CloudFront Distribution Cache Behavior Path Pattern: hello world.jpg
```

thrown by `NextjsDistribution.addStaticBehaviors` while validating a path pattern
per `public/` entry. The fixture's `public/` holds `hello world.jpg` and
`äöüščří.png`; both are ordinary Next.js assets that `next start` serves and
`next/image` points at.

A CloudFront path pattern's alphabet is `A-Z a-z 0-9 _ - . * $ / ~ " ' @ : +` and
`&`. A space is not in it — and neither is `%`, so the percent-encoded form the
request actually arrives as (`/hello%20world.jpg`) cannot be written out either.
Throwing was the only thing cdk-nextjs did with that, which made an app that
Next.js supports undeployable.

**Verdict: fixed.** `toPathPattern` (`src/nextjs-distribution.ts`) replaces each
character outside the alphabet with one `?` per character of its encoded form —
`?` matches exactly one character, so `hello world.jpg` becomes
`hello???world.jpg` and `äöüščří.png` becomes 42 `?` and `.png`. That is the
narrowest pattern CloudFront can express for those names. A `*` would have been
shorter and far wider: `äöüščří.png` would collapse to `*.png`, pulling every
`.png` request in the app onto the static origin. A name whose pattern would
exceed CloudFront's 255-character limit still throws, now naming the file and the
length.

The regional Functions type has the same problem one layer down: an API Gateway
resource path part only allows `[a-zA-Z0-9:._-$]`, and `addResource` throws on the
same names — with a CDK message that names no file. `NextjsApi` now warns at synth
and skips the entry, so the app deploys and only that asset 404s. Warning rather
than throwing follows `warnOnTrailingSlashGroups` in the same construct: the
resource tree cannot express it, and failing the synth is worse than saying so.

Covered by three `nextjs-distribution.test.ts` cases (a file, a directory, and the
length limit) and one in `nextjs-api.test.ts`.

Batch 13 confirmed this half: the fixture built and deployed, and 4 of its 5 cases
passed, both unicode ones included. The fifth was a second defect in the same file —
`/_next/image` for the space-bearing name, defect 24 below.

### 19. A throw out of a route answered plain text, never the app's error page

`async-modules` (1 of 7, "can render async error page"). `/make-error` throws in
`getServerSideProps`; the fixture ships a `pages/_error` rendering
`<p id="content-error">hello error</p>`, and the browser got
`500 Internal Server Error` as `text/plain` instead.

Next.js's page handlers catch, report and then deliberately rethrow — "rethrow so
that we can handle serving error page", `pages-handler.ts` — which makes rendering
the error page the host's job, exactly as `render404` makes the 404 the host's job.
cdk-nextjs had no counterpart: every throw reached one `failWith` that wrote a bare
plain-text 500, so a `pages/_error`, a `pages/500`, and the prerendered `500.html`
next emits by default were all dead code. Not specific to this fixture — it was
every unhandled error in every app.

**Verdict: fixed.** `resolveErrorTarget` (`src/runtime/dispatch.ts`) resolves the
ladder once from the manifest, in the order `base-server.ts`'s
`renderErrorToResponse` uses: an invocable `/500`, then the prerendered `/500`
(`pages/500.js` is a `STATIC_STATUS_PAGES` entry, so it usually arrives as HTML),
then `/_error`, then nothing. `NextjsRuntime.sendError` walks it, sets the 500
*before* invoking so `_error`'s `getInitialProps` reads the right status, and
renders for the URL that was asked for rather than for `/_error`. It is resolved on
the runtime rather than on the Dispatcher, which is per request: the throw this
answers can happen before one exists.

Three things the fallback path now also does: it survives an error page that throws
in turn, without recursing; it strips a `Content-Length` and `ETag` the render that
threw may have left describing a body that never arrived; and it sends
`Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate`, so a
long-lived `Cache-Control` from the failed render cannot get a 500 cached at the
edge.

Covered by four `resolveErrorTarget` cases in `dispatch.test.ts` and four in
`core.test.ts` (the prerendered `/500`, the `/_error` invocation, an error page that
throws, and an app with neither).

### 20. Every i18n app answered its own home page with a redirect

`i18n-support-catchall` (1 of 4, "should load the index route correctly SSR").
`next.fetch('/', { redirect: 'manual' })` expects a **200** and got a **308** to
`/en-US`. Not specific to the fixture's root catch-all: it was every app with an
`i18n` config, at `/`.

`@next/routing` builds the locale-prefixed pathname by concatenation —
`` `${basePath}/${locale}${pathname}` `` — which for the root produces `/en-US/`.
`next build` always compiles a `priority` 308 that strips a trailing slash into
`routing.beforeMiddleware`, that route then matched the path the prefixing had just
invented, and the redirect went out before anything looked for a page. Next.js's own
router special-cases exactly this shape (`resolve-routes.ts`:
`` pathname === '/' ? `/${defaultLocale}` : … ``).

Measured with `scripts/e2e-offline.sh`: `next start` on a plain build of the fixture
answers `/`, `/en-US`, `/fr`, `/nl-NL` and `/another` with 200, and `/` with
`accept-language: nl` with a 307 to `/nl`. Our deployment redirected `/`, which is
also why only 1 of the 4 cases failed — the three that navigate follow the redirect
and land on the right page.

The same concatenation is how `resolveRoutes` builds the *location* of the
locale-detection 307, so that redirect was wrong twice over: it sent
`https://example.test/nl-NL/` where `next start` sends `/nl-NL` — a needless second
round trip through the slash-stripping 308, and a value no test asserting
`headers.location` can match.

**Verdict: fixed.** Two narrow pieces in `src/runtime/dispatch.ts`:

- `Dispatcher.withRootLocale` prefixes the locale itself for a request to the app's
  root, and only when the locale the request already asks for is the (domain-aware)
  default — i.e. when no redirect is owed. `resolveRoutes` then sees a locale in the
  path and leaves it alone. A root request whose detected locale is *not* the default
  is passed through untouched, because `resolveRoutes` answers it with the 307 after
  middleware has had the request, and that ordering is Next.js's.
- `Dispatcher.normalizeRedirectLocation` puts every same-origin location in the form
  Next.js sends: a path rather than an absolute URL, minus the stray trailing slash a
  locale-root redirect carried. Cross-origin locations — the `i18n.domains` redirect
  to another locale's domain — are left absolute, since that is the only way to
  leave the origin.

Covered by four cases against the `pages-i18n` fixture in `dispatch.test.ts` (the
root with no redirect, the detection 307, the cross-domain 307, and the trailing
slash a request really did carry). One existing expectation changed with it: a
middleware `NextResponse.redirect()` to a same-origin URL now reports the path,
which is what Next.js puts on the wire.

### 21. A static `getStaticProps` page's data route 404'd, so client-side navigation into it reloaded the page

`no-page-props` (1 of 5, "should navigate between pages correctly"). The case clicks
a link to `/gsp`, then reads text off the page and got `undefined` where `"hi"` was
expected.

Measured with `scripts/e2e-offline.sh` against `next start` on a plain build of the
same fixture:

```
ours   /_next/data/<buildId>/gsp.json   404 text/html   (the 404 page)
oracle /_next/data/<buildId>/gsp.json   200 application/json  {"__N_SSG":true,"pageProps":{"hello":"world"}}
both   /_next/data/<buildId>/gssp.json  200 {"__N_SSP":true,…}
```

So `/gssp` (a `getServerSideProps` page, which is an output in its own right) worked
and `/gsp` did not. The Pages Router fetches that `.json` on every client-side
navigation; a 404 makes it fall back to a full page load, which loses the
client-side state the assertion reads.

`next build` emits a `dynamicRoutes` rule for a data route only when the page is
dynamic **or** the app has middleware (`build-complete.ts`,
`needsMiddlewareResolveRoutes` — the same flag it reports to us as
`routing.shouldNormalizeNextData`). Without middleware, a *static* `getStaticProps`
page's data route arrives only as a concrete `prerenders` pathname: no template to
match, no rule of its own, and Next.js expects the platform to serve it as an output
keyed by pathname. `addPrerenderPathnames` was skipping exactly that shape.

**Verdict: fixed.** `addPrerenderPathnames` (`src/adapter/build-outputs.ts`) now
registers a concrete `/_next/data/<buildId>/…json` prerender against its owning
route's entrypoint whenever no *ungated* rule matches it. The "ungated" narrowing is
load-bearing: a dynamic page's `/_next/data/<buildId>/en-US/blog/hello.json` is
reached by the `…/blog/[slug].json` rule, and that rule is where the `nxtPslug` param
comes from — registering the concrete pathname too would resolve the request to
itself and drop the param. Verified offline (`gsp.json` and `gssp.json` both 200,
matching the oracle) and covered by a `build-outputs.test.ts` case that asserts both
halves.

### 22. An absolute `assetPrefix` with a path 404'd every bundle

`asset-prefix-absolute` (1 of 1, "bundles should return 200 on served
assetPrefix"). The fixture sets `assetPrefix:
'https://example.vercel.sh/custom-asset-prefix'`, collects the `<script src>`s the
page emits, and re-requests each one's *path* against the deployment — the test
helper keeps only `pathname`/`search` from an absolute URL. It expected 200 and got
404.

The premise is real, and `next build` is what makes it so. With a path-carrying
`assetPrefix`, absolute or not, it compiles a rewrite of its own into
`beforeFiles`:

```
/custom-asset-prefix/_next/:path+  →  /_next/:path+
```

Measured with `scripts/e2e-offline.sh` against `next start` on a plain build:
`/custom-asset-prefix/_next/static/chunks/<name>.js` is **200
application/javascript**, `/custom-asset-prefix/bogus/_next/static/...` is 404, so
the prefix really is stripped rather than ignored. The rewrite is in our manifest
too (`routing.beforeFiles`, persisted verbatim), so the runtime handles it — but
`_next/static` never reaches the runtime in a deployment: those objects are in S3,
and CloudFront had no behavior for `/custom-asset-prefix/*`, so the request fell
through to the compute origin, which carries no `.next/static` at all.

**Verdict: fixed.** `NextjsDistribution` already adds a `<assetPrefix>/_next/static*`
behavior with a CloudFront Function that rewrites the prefix back to the S3 keys —
it just never saw this prefix, because `readNextConfigAssetPrefix` reduced every
absolute `assetPrefix` to `""`. Split in two:

- `readNextConfigAssetPrefix` keeps its meaning (a path-style prefix, `""` for an
  absolute one) and keeps driving the regional-`NextjsType` warning, which should
  *not* fire for an absolute prefix — pointing one at a CDN you front the assets
  bucket with is the documented way to serve assets from elsewhere.
- `readNextConfigAssetPrefixPath` is new and reports the path portion of either
  form, and it is what the Global root constructs hand the distribution.

So `https://cdn.example.com/cdn` now behaves as `/cdn` while
`https://cdn.example.com` still adds nothing. The sibling `asset-prefix-absolute-no-path`
fixture (`assetPrefix: 'https://example.vercel.sh/'`) is the second case: its path is
`/`, which normalizes to none, and its bundles are requested at plain
`/_next/static/...`, which the existing behavior already serves.

Covered by `nextjs-distribution.test.ts` (a behavior and the right slice length for
both spellings of the absolute-with-path form; still nothing for the no-path and
basePath-equal ones) and `base-path.test.ts` (the two readers disagreeing on purpose).
README's `assetPrefix` section gained the case.

### 24. `/_next/image` 400'd for a `public/` file whose name needs percent-encoding

`next-image-legacy/unicode`, the other half of defect 18. That fix got the app
deployable — the file's CloudFront path pattern — and 4 of the fixture's 5 cases
went green with it, including both unicode ones. The fifth stayed 400:

```
/_next/image?url=%2Fhello%2520world.jpg&w=640&q=75   →  400
/_next/image?url=%2F%C3%A4%C3%B6%C3%BC%C5%A1%C4%8D%C5%99%C3%AD.png&…  →  200
```

The `url` query value is double-encoded, so decoding the query string leaves one
layer: `validateParams` hands `fetchFromS3` the href `/hello%20world.jpg`, and the
object's key is `hello world.jpg`. `fetchFromS3` used the href as the key verbatim,
missed, and `resolveErrorResponse` mapped `NoSuchKey` to the 400 a missing local
image is supposed to get — so the symptom looked exactly like a genuinely absent
file. The unicode sibling passed because `äöüščří` is *not* percent-encoded inside
the `url` value: the query decode leaves the raw characters, which are already the
object's name. Next.js's own `fetchInternalImage` never has this problem — it makes
an HTTP subrequest and its static file server resolves the path against the
filesystem, which decodes on the way.

**Verdict: fixed.** `fetchFromS3` (`src/runtime/image-utils.ts`) percent-decodes the
asset path before joining the bucket key prefix, tolerantly: a path that is not
valid percent-encoding is used as given, because that is what a literal `%` in a
filename looks like (`public/100%.png` is requested as `/100%.png`, which browsers
do not escape and `decodeURIComponent` throws on). The `basePath` strip still
happens before the decode, since both sides of that comparison are URL-space
values. Four `image-utils.test.ts` cases cover the space, both unicode spellings
and the literal `%`.

## Bug — not yet fixed

### A path-traversal `_next/data` request 500s instead of rendering

`incremental-cache-path-traversal` (1 of 1). **Verdict: bug**, low severity.

The file is upstream's regression test for a path-traversal read: it requests
`/_next/data/<buildId>/pages-cache/..%2F..%2Fserver-reference-manifest.json` and
asserts a **200** whose `pageProps.rest` is the literal `['..', '..',
'server-reference-manifest']` — i.e. the traversal is treated as ordinary route
params and no manifest is leaked. We answer **500**.

So the security property the test exists to protect holds — nothing leaks — and
the failure is a fidelity gap: the runtime throws where `next start` renders.
Worth fixing for that reason, and worth noting that the file is *not* screened
out despite its `describe.skip`: that skip is conditional on
`__NEXT_CACHE_COMPONENTS`, which the harness does not set.

## Failing — awaiting a verdict

Batch 9 (23 files, 2026-09-22) produced 11 green files, 1 no-signal
(`app-fetch-deduping`, already screened out as `skipDeployment`) and 11 failures.
Four of the eleven are accounted for and requeued rather than diagnosed:

- **Three failed wholesale on a `createNext` timeout**, not on an assertion:
  `script-loader`, `src-dir-support` and `trailing-slashes-href-resolving` all
  threw `Exceeded timeout of 240000 ms for a hook` out of `beforeAll`, 39 times
  between them, and were the three slowest files in the run (1153s, 985s, 635s).
  The deploy simply did not finish inside `NEXT_E2E_TEST_TIMEOUT`. This is the
  cost problem the cache bucket's churning `Tags` caused — ~110s of full
  CloudFormation per file where a hotswap is ~52s — so all three are retried
  rather than diagnosed. No adapter signal either way yet. Read their duration,
  not their case names: `src-dir-support` reports 8 of 8 "routing" cases failing
  and none of them ran.
- **`app-dir/mdx` (2 of 27)** was the harness reporting the wrong
  `DEPLOYMENT_ID`: the marker line said `$STACK_NAME` where next.js compares
  against the `?dpl=` the *build* inlined. Fixed in `scripts/e2e-deploy.sh`;
  requeued to confirm.

Six of the remaining seven turned out to be one defect — every Pages Router app's
home page 404'd, see defect 17 above. They are queued for a deployed re-run and are
not in `rules.include` until they come back green.

Batch 10 (20 files, 2026-09-23, including batch 9's three timeouts and `mdx` as
retries) produced 15 green files and 5 failures. `mdx` passed, which confirms the
`DEPLOYMENT_ID` marker fix; the three timeout files did not make it into this batch
and are still requeued. All five failures are defect 17 — the run was deployed
before that fix was bundled — except for one case in `async-modules`, below.

Batch 11 (50 files, 2026-09-23, the first run with defect 17's fix bundled)
produced 32 green files, 13 no-signal ones (the helper-hidden `skipDeployment`
files, below) and 5 failures. One of the five is defect 18 — `next-image-legacy/unicode`
never deployed at all — and it is requeued. That left four files awaiting a verdict,
two of them carried over from batch 10; all four were root-caused while batch 12 ran,
as defects 19 through 22, and all four left this list without a deployment.

`async-modules` went first: batch 11 had already cleared its other two failures (defect 17), which isolated its
remaining case enough to root-cause, and it is defect 19 above.
`i18n-support-catchall` followed — it was never about the fixture's root catch-all
but about `/` under any `i18n` config, reproduced in a unit test against the
`pages-i18n` fixture and confirmed against `next start` with
`scripts/e2e-offline.sh`, and it is defect 20. `no-page-props` went the same way —
one `.json` request compared against a plain `next start` build was the whole
diagnosis, and it is defect 21. All three are requeued behind a bundle.

`asset-prefix-absolute` went last, and it was the one open question of the four:
it sets an *absolute* `assetPrefix` pointing at a second origin, which
`NextjsDistribution` deliberately added no behavior for, so "defect or fixture
assuming a deployment shape cdk-nextjs does not provide" was a real fork. `next
start` settled it — the prefix's *path* is served, because `next build` compiles a
rewrite for it — and it is defect 22.

Batch 12 (50 files, 2026-09-23, all of them new candidates) is the first batch to
come back **entirely green** — 50 of 50 on attempt 0, no failures, no no-signal
files, `exiting with code 0`. That is what defects 17 through 22 bought: the
batch ran across CSS Modules and Lightning CSS, every `global-not-found` variant,
the five `metadata-static-file` route shapes, MDX, `modularizeImports`, Monaco and
`next/dynamic` without one adapter error. All 50 are in `rules.include` above.

Batch 13 (50 files, 2026-09-23 — the nine requeued/regression files behind defects
18 through 22, plus 41 new candidates) produced 49 green files and one failure.
Defects 19, 20, 21 and 22 are now **verified against a real deployment**:
`async-modules`, `i18n-support-catchall`, `no-page-props` and
`asset-prefix-absolute` all passed on attempt 0, as did the `app-basepath`,
`asset-prefix` and `not-found-with-pages-i18n` regression checks.

The one failure is `next-image-legacy/unicode`, and it is progress rather than a
regression: defect 18 got the app deployable (it never built before) and 4 of its 5
cases went green, leaving one 400 that turned out to be a second, separate defect in
the image optimizer's S3 key — defect 24 above. It is requeued into batch 14.

No file is awaiting a verdict as of 2026-09-23; nothing is in `excluded-notes` for
want of a decision.

## Unsupported — a product limitation

### `dynamicParams = false`

`app-dir/prerender-encoding` (1 of 1 case). **Verdict: unsupported.** Not an
encoding bug, despite the file's name and the shape of the failure.

The fixture prerenders the param `sticks & stones` via `generateStaticParams`
and requests `/sticks%20%26%20stones`; cdk-nextjs returns the 404 page. Encoding
turned out to be fine — dispatching that path against the adapter manifest
resolves `nxtPid` to exactly `sticks & stones`. The trigger is one line in the
fixture: `export const dynamicParams = false`.

With that flag, Next.js emits the route's `routing.dynamicRoutes` entries gated
behind preview cookies, because the platform is expected to serve the concrete
prerendered paths off the CDN itself and 404 everything else:

```
has: [{type:'cookie', key:'__prerender_bypass', value:…},
      {type:'cookie', key:'__next_preview_data'}]
```

cdk-nextjs registers only dynamic *templates* in `manifest.pathnames`
(`addPrerenderTemplates` in `src/adapter/build-outputs.ts`), so nothing matches
the gate and every param 404s — `/plain` as much as the encoded one. Removing
the flag from a copy of the fixture makes both resolve:

```
dynamicParams = false   "/plain"                 → not-found
                        "/sticks%20%26%20stones" → not-found
flag removed            "/plain"                 → /[id]  nxtPid="plain"
                        "/sticks%20%26%20stones" → /[id]  nxtPid="sticks & stones"
```

Supporting it means routing concrete prerendered paths to the owning entrypoint
*while still supplying the template's `nxtP*` params* — which is precisely what
`addPrerenderTemplates`'s own comment records as measured-wrong when done
naively (`/isr/1` then resolves to itself rather than to `/isr/[id]`, losing the
`nxtPid` the route needs). A design change, not a patch, and worth doing only if
a user asks.

Incidentally measured while chasing this: an unencoded `&` in a path is parsed
as a query separator (`/a&b` → `{nxtPid: "a", b: ""}`), which matches Vercel.

## Unsupported — the edge runtime

cdk-nextjs refuses at build time to deploy any output whose runtime is not
`nodejs` (`assertNodeRuntimes` in `src/adapter/build-outputs.ts`). That is a
deliberate product limitation: the edge runtime is deprecated in Next.js, and
cdk-nextjs supports Next.js 16's Node-runtime `proxy.ts` instead of the legacy
edge-only `middleware.ts`.

The check throws during `next build`, so a fixture containing *any* edge route or
legacy middleware fails **wholesale** — per-case `failed` entries in the manifest
cannot rescue it, and the file has to stay out of `rules.include`. 203 of
next.js's 1134 e2e files are disqualified this way, leaving 931 edge-free, so it
is not much of a constraint on widening the list.

Both spellings count: app router's `export const runtime = 'edge'` and pages
router's `export const config = { runtime: 'experimental-edge' }`. Screening only
the first undercounts by ~17 files, which is how an earlier draft of this file
came to claim ~612 edge-disqualified and ~522 edge-free. Neither figure was
measured; both are replaced above.

Not enumerated file by file. Three worth naming:

| File                                 | Why                                            |
| ------------------------------------ | ---------------------------------------------- |
| `app-dir/app-static`                 | ~10 `*-edge` routes declare `runtime = 'edge'`. Worth revisiting as a source of individual cases to port — it is the densest coverage of the ISR/caching behavior cdk-nextjs's cache handler implements. |
| `app-dir/actions/app-action`         | Fixture ships a legacy edge `middleware.js`.   |
| `middleware-rewrites/test/index`     | Fixture ships a legacy edge `middleware.js`.   |

## CDN-inherent — excluded, acceptable

### CloudFront rejects unencoded `[` and `]` in a path

`dynamic-route-interpolation` (4 of 7, the other 3 included via `suites`).

The four request a path whose square brackets are literal, not a dynamic segment
— `/blog/[slug]`, `/api/dynamic/[abc]`. CloudFront answers before the Lambda is
reached at all:

```
$ curl -sg --path-as-is -D - -o /dev/null 'https://…/blog/[slug]'
HTTP/2 400
content-length: 0
x-cache: Error from cloudfront
```

Percent-encoded, the same routes are correct — `/blog/%5Bslug%5D` renders
`[slug]` into `#slug`, and `/api/dynamic/%5Bslug%5D` returns `slug: [slug]` — so
routing and param interpolation work; it is the wire format CloudFront refuses.
`[` and `]` are `gen-delims` reserved for IPv6 literals in the authority and are
not legal in a path, so this is conformant of it. The 3 remaining cases (a normal
param, and two data-cache busting cases) pass and are included.

### Revalidation re-requested about a second later

`app-dir/revalidate-dynamic` (2 of 2), `app-dir/revalidate-path-with-rewrites`
(1 of 2) and `app-dir/trailingslash` (2 of 8, the rest included via `suites`).

Each loads a prerendered page, calls a route handler that runs
`revalidatePath`/`revalidateTag`, asserts the handler returned
`revalidated: true` (it does), refreshes, and expects new content. The refresh is
served from the CloudFront edge: a prerendered app-router page comes back
`cache-control: s-maxage=31536000`, and the second request to it reports
`x-cache: Hit from cloudfront` (verified by hand).

After defect 9 the edge *is* invalidated, including for a build-time prerender —
but `CreateInvalidation` is fire-and-forget and has no bounded completion SLA.
What decides whether a case survives that is how long it is willing to wait, and
the discriminator is measurable: `retry()` in `test/lib/next-test-utils.ts`
defaults to `duration = 3000`. `segment-cache/prefetch-inlining` passes `15000`
and now passes; these files take the 3s default, or re-request immediately, and
an invalidation does not land in 3s. Vercel purges its own CDN synchronously as
part of the revalidate, which is why the tests are not gated out of deploy mode
upstream.

Not a defect, and not worth chasing — making it pass would mean blocking every
revalidation on an invalidation that usually takes tens of seconds.

### Stale-while-revalidate after a tag revalidation

`app-dir/resume-data-cache` (2 of 5; the other 3 are included via `suites`).
**Verdict: architectural** — unreachable for any distributed cache handler, ours
included, and the failing assertion is that cdk-nextjs is *too fresh*.

Both failures are the `it.each` cases, "should have consistent data between
static and dynamic renders with $name". Each revalidates a tag and then asserts
the next dynamic RSC request still returns the **stale** value, once, before the
refreshed one. Next.js tracks tag staleness in
`tagsManifest`, a plain in-process `Map` in
`packages/next/src/server/lib/incremental-cache/tags-manifest.external.ts`;
`areTagsStale` reads only that Map. A cache handler has no channel for "this tag
went stale, serve it anyway": `lastModified: -1` forces a blocking render, and so
does returning `null` — for a route with no fallback. So a revalidated tag is
immediately fresh across our fleet rather than stale-then-fresh. Fixing it would
mean Next.js exposing tag staleness to the handler interface.

(The two are *not* interchangeable for a route that has one. Defect 23 in
`docs/plans/adapter-runtime-progress.md`: a hard miss on a PPR route whose
prerender-manifest entry carries a `fallback` is answered from the fallback shell,
uncacheable and never upgraded, where `lastModified: -1` renders and stores. Our
handler now uses the latter for every response entry.)

The "fetch cache" case would fail a second way even without that: it prefetches
the same URL the "use cache" case prefetched, with an identical `_rsc`
cache-busting hash, and CloudFront answers with the earlier case's payload. That
is the `cdn-cache-busting` problem next.js gates out itself.

The remaining 3 cases — RDC across a server-action re-render, and `updateTag` in
a server action for both cache kinds — do pass and are worth having, so the file
is in `suites` with these two excluded.

## No signal — next.js skips these itself

| File                                    | Why                                                                                                                                  |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `app-dir/segment-cache/refresh`         | `describe.skip` upstream, commented `Disabled because too flaky`. Recorded so it is not re-screened: it reports "passing" in ~5s without deploying anything. |
| `app-dir/segment-cache/cdn-cache-busting` | next.js skips it under `isNextDeploy`. Listed as a worked example of deploy mode gating out what cannot work behind a CDN.           |
| `app-dir/app-fetch-deduping`            | Branches on `isNextStart` / `isNextDev` only; deploy mode gets `it('should skip other scenarios')`. Measured at 4s, no deploy.       |

### `skipDeployment: true` — the same trap, and it had cost us two rows

`nextTestSetup({ skipDeployment: true })` makes next.js replace the whole file
with `it.only('should skip next deploy')` and set `skipped`, so the body
early-returns (`test/lib/e2e-utils/index.ts`). jest reports a pass in about four
seconds, having deployed nothing.

This was not screened for until now, and two files had been listed in
`rules.include` on the strength of such a pass — `app-dir/not-found-default` and
`app-dir/use-cache-search-params`. Both have been removed; neither had ever run.
Nine more were spent in one batch before the screen existed: `404-page-ssg`,
`500-page`, `app-client-cache/client-cache.original`, `app-fetch-deduping`
(a different gate, above), `app-prefetch/prefetching.stale-times`,
`binary/rsc-binary`, `draft-mode`, `dynamic-optional-routing`, `gssp-redirect`,
`index-index`, `route-indexes`.

`screen.mjs` now disqualifies it, which took the candidate pool from 706 to 465 —
a third of it was never going to yield anything. The general rule, now recorded in
the log-reading section below: **a file that "finishes" in under ~10 seconds
deployed nothing.**

### …and `skipDeployment: true` is not always in the test file

The screen above read the test file's own text, which missed thirteen more files —
every `app-dir/cache-components-errors/*` in batch 11. They call `nextTestSetup`
from a sibling `shared.util.ts`, or they *are* a two-line
`require('./client.test')` wrapper around a file that does, so the literal
`skipDeployment: true` is one module away. All thirteen reported a pass in 3.6–4.2s
having deployed nothing:

`client.partial-prefetching`, `error-attribution`,
`error-attribution.partial-prefetching`, `metadata-and-viewport`,
`metadata-and-viewport.partial-prefetching`, `sync-dynamic`,
`sync-dynamic.partial-prefetching`, `sync-io-node-crypto`,
`sync-io-node-crypto.partial-prefetching`, `sync-io-time-and-random`,
`sync-io-time-and-random.partial-prefetching`, `use-cache`,
`use-cache.partial-prefetching`.

`screen.mjs` now follows relative `import`/`require` specifiers two levels deep for
this one screen — the two shapes above are what needs two levels — which took the
candidate pool from 373 to 341 and saved roughly 72 minutes of deploys that would
have proved nothing. Following the import cannot over-report: `skipDeployment: true`
in a shared helper replaces *every* file that calls it. The mode gates above are
deliberately still test-file-only, because a helper's `isNextDev` branch may cover
only some of a caller's cases.

### `skipDeployment: !isAdapterTest` is not the same thing

Ten e2e files gate themselves on `process.env.NEXT_ENABLE_ADAPTER === '1'`, most
of them as `nextTestSetup({ skipDeployment: !isAdapterTest })`. Left unset, they
take the `skipDeployment` path above and report a pass in ~5s having deployed
nothing — which is exactly how `partial-fallback-shell-upgrade` "passed" in 5.361s
in one batch and then produced four real failures in the next.

cdk-nextjs *is* an adapter deployment, so the harness now exports
`NEXT_ENABLE_ADAPTER=1`. The only other thing that reads it is the Vercel deploy
path's team and token selection (`test/lib/next-modes/next-deploy.ts`), which a
custom deploy script never reaches. `screen.mjs` deliberately does **not** treat
`skipDeployment: !isAdapterTest` as a disqualifier, because with the flag set
those files do run.

### `NEXT_PRIVATE_TEST_MODE` is what makes a PPR response splittable

The PPR tests do not assert on a response body; they assert on its *two halves*.
`splitResponseWithPPRSentinel` (`test/lib/e2e-utils/ppr.ts`) buffers the whole
body and splits it on the literal string `<!-- PPR_BOUNDARY_SENTINEL -->`, then
hands back `static$` and `dynamic$`. If the sentinel is absent, `parts[1]` is
`undefined`, *everything* lands in `staticPart`, and every
`expect(result.static$('#something-dynamic').length).toBe(0)` fails.

The sentinel is emitted by `packages/next/src/build/templates/app-page-runtime.ts`
under `if (process.env.__NEXT_TEST_MODE)`, and `__NEXT_TEST_MODE` is a build-time
constant: `define-env.ts` inlines it, so unset it inlines as `false` and the
branch is dead-code eliminated. The harness appends a snippet to every fixture's
`next.config` aliasing `NEXT_PRIVATE_TEST_MODE` → `__NEXT_TEST_MODE`
(`test/lib/next-modes/base.ts`), and Vercel supplies it as `--build-env
NEXT_PRIVATE_TEST_MODE=e2e`. `scripts/e2e-deploy.sh` now exports the same thing.

Until it did, `partial-fallback-root-blocking` (1 of 1),
`partial-fallback-shell-upgrade` (4 of 7) and `sub-shell-generation` (6 of 7) all
failed with one shape — elements that belong only to the dynamic part showing up
in the static one. That shape was not a cdk-nextjs defect; it was a missing build
env var.

It was, however, masking one. With the var exported, `sub-shell-generation` still
failed 6 of 7, now on a different assertion — the shell's own sentinel — which is
defect 14. A missing build env var and a real defect can share a file; the tell is
that the *shape* of the failure changed.

## Reading a harness log

Two traps, both of which have cost time.

A file that fails **wholesale** at around 120s, with
`thrown: "Exceeded timeout of 120000 ms for a hook."` in `beforeAll`, did not
fail: the CloudFormation deploy outran jest's hook timeout. That is what
`--retries 1` is for. Read the retry, not attempt 0.

And in the other direction: a file that **passes in under ~10 seconds deployed
nothing**. A real file spends 100s+ on `next build` plus a hotswap. A 4s pass
means `skipped` — `describe.skip`, `skipDeployment: true`, or a deploy-mode branch
that is just a stub — and adding it to `rules.include` claims coverage that does
not exist. `run-tests.js --timings` prints the duration per file; it is the only
signal in the log for a file that passes, since output is printed on failure only.

[harness]: https://nextjs.org/docs/app/api-reference/adapters/testing-adapters
[oac]: https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-restricting-access-to-lambda.html
